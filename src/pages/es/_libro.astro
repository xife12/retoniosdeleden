---
/**
 * Archiviert: das Buch ist vorerst nicht veröffentlicht.
 *
 * Der Unterstrich im Dateinamen hält Astro davon ab, hier eine Route zu
 * bauen — die Seite bleibt vollständig erhalten, ist aber nicht erreichbar.
 * Zum Wiederveröffentlichen zurück nach `libro.astro` umbenennen und in
 * `src/config.ts` `libroActivo` auf `true` setzen.
 */
import BookPage from '../../components/libro/BookPage.astro';
---

<BookPage lang="es" />
